const chat = (name) => {
    return (
        <>
            {/* This button will need to take the user to the designated chat. Likely need some function to take
            them to that page*/}
            <button>{name}</button>
        </>
    )
}